# ========================================
# HOSTAL API - Configuración de Entorno
# ========================================
# Copia este archivo como .env y ajusta los valores según tu entorno

# --- Entorno de Aplicación ---
APP_ENV=dev
# Opciones: dev, staging, prod
# En prod se desactiva DEBUG automáticamente y se valida SECRET_KEY

DEBUG=True
# Solo para desarrollo, nunca en producción

# --- Seguridad ---
SECRET_KEY=change-me-in-production
# CRÍTICO: En producción, genera una clave segura con:
# python -c 'import secrets; print(secrets.token_urlsafe(32))'
# Mínimo 32 caracteres

ALGORITHM=HS256
# Algoritmo para tokens JWT, HS256 es el estándar

ACCESS_TOKEN_EXPIRE_MINUTES=120
# Duración de los tokens JWT en minutos

# --- Configuración del Servidor ---
API_URL=http://localhost:8000
# URL base del servidor API (sin trailing slash)
# Usado para generar URLs absolutas de archivos multimedia
# Para desarrollo: http://localhost:8000
# Para producción: https://api.tudominio.com

# --- CORS (Cross-Origin Resource Sharing) ---
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
# Lista separada por comas de orígenes permitidos
# Para desarrollo local: http://localhost:3000,http://localhost:5173
# Para producción: https://tudominio.com,https://www.tudominio.com
# Para permitir todos (NO RECOMENDADO): *

# --- Google OAuth Configuration ---
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID_HERE
# Obtén en: https://console.cloud.google.com/
# ID público, puede estar en el código del cliente

GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET_HERE
# CRÍTICO: Mantén esto secreto, nunca lo subas a git
# Obtén en: https://console.cloud.google.com/

# --- Base de Datos PostgreSQL ---
# Opción 1: URL completa (recomendado)
# DATABASE_URL=postgresql+psycopg://usuario:password@host:puerto/database

# Opción 2: Componentes individuales (se construye automáticamente)
POSTGRES_USER=hostal
POSTGRES_PASSWORD=hostal_pass
POSTGRES_DB=hostal_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# --- Configuración de Red (opcional) ---
# NETWORK_DEBUG=1
# Habilita logs de debug para operaciones de whitelist de dispositivos

# --- Notas para Producción ---
# 1. Cambia SECRET_KEY a un valor seguro aleatorio
# 2. Configura APP_ENV=prod
# 3. Usa contraseñas fuertes para PostgreSQL
# 4. Especifica CORS_ORIGINS con tus dominios reales
# 5. Considera usar variables de entorno del sistema en lugar de .env
# 6. Nunca subas el archivo .env al repositorio (debe estar en .gitignore)
